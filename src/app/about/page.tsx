import { InfoPage } from "@/components/marketing/info-page"; import { metadataFor } from "@/lib/site"; export const metadata=metadataFor("/about");
export default function Page(){return <InfoPage label="About" h1="How ImageFinisher Handles Your Files" lede="ImageFinisher shows you what is in the file, removes only the fields it supports, and treats visual editing as a separate job. The tool never claims more than it can prove."><h2>Why everything starts in your browser</h2><p>You should not have to upload prompts, GPS coordinates, or other private details just to check an image. The entire file session stays in your browser, and we create a separate copy only when you choose to clean it.</p><h2>Why we do not show an AI score</h2><p>Metadata can tell you which tools, workflow details, or provenance records are attached to a file. It cannot give every image a meaningful “AI probability,” and it cannot predict how every platform will classify it.</p><h2>What we are still working on</h2><p>Visual repair is not available yet. We will add it only after a real provider workflow meets our standards for quality, image preservation, speed, and cost. Until then, we will not dress up a static filter or demo image as a real result.</p></InfoPage>}
