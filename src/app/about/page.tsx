import { InfoPage } from "@/components/marketing/info-page"; import { metadataFor } from "@/lib/site"; export const metadata=metadataFor("/about");
export default function Page(){return <InfoPage label="About" h1="A More Honest Image Handoff Workflow" lede="ImageFinisher separates file evidence, safe cleaning and visible image quality so each claim stays inside what the product can prove."><h2>Why local-first comes first</h2><p>Prompts, GPS and private image details do not need to cross a network for a metadata check. Phase 1 keeps the file session inside the browser and makes a separate copy only when you choose to clean.</p><h2>Why there is no AI score</h2><p>Metadata can describe tools, provenance and workflow. It cannot support a universal probability that an image is “AI,” and it cannot predict every platform classifier.</p><h2>What comes later</h2><p>Visual repair will remain unavailable until a real provider workflow passes quality, preservation, latency and cost evaluation. No static filter or sample is presented as a processed user result.</p></InfoPage>}
