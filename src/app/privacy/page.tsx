import { InfoPage } from "@/components/marketing/info-page"; import { metadataFor } from "@/lib/site"; export const metadata=metadataFor("/privacy");
export default function Page(){return <InfoPage label="Privacy" h1="Your Image Stays in This Browser Session" lede="Inspection, cleaning, and verification all happen locally. Here is exactly what that means for your files."><h2>Your files stay local</h2><p>The tool does not upload image data, filenames, thumbnails, prompts, GPS coordinates, or raw EXIF values. Preview links exist only in the current browser session. They are removed when you delete a file from the workspace or close the page.</p><h2>Local session diagnostics</h2><p>The workspace keeps up to 200 coarse events in page memory: selection, scanning, cleaning, verification and download actions. Events contain only a known page path, format, sample-or-file source, size/count/time ranges, mode, outcome and approved error codes. They contain no filenames, prompts, coordinates, image hashes, thumbnails or raw metadata. The log is lost when the page session ends. No remote analytics receiver is configured in this version.</p><h2>No cloud processing yet</h2><p>This version does not upload images to a cloud AI service. Any future cloud feature will ask for your consent first and create a sanitized processing copy locally before uploading anything private.</p></InfoPage>}
