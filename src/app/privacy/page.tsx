import { InfoPage } from "@/components/marketing/info-page"; import { metadataFor } from "@/lib/site"; export const metadata=metadataFor("/privacy");
export default function Page(){return <InfoPage label="Privacy" h1="Your Image Stays in This Browser Session" lede="Phase 1 inspection, cleaning and verification are local. This page states the boundary plainly."><h2>Local file handling</h2><p>Image bytes, file names, thumbnails, prompts, GPS values and raw EXIF values are not uploaded by the tool. Object URLs exist only for the current preview and are revoked when a file is removed or the workspace closes.</p><h2>What diagnostics may contain</h2><p>Local errors use technical codes and safe messages. If analytics is added later, it may contain a page path, file-count range, format, processing-time range or error code—but never the image, filename or raw metadata.</p><h2>Cloud features are not active</h2><p>There is no cloud AI upload in Phase 1. A future cloud feature will require explicit consent and a locally sanitized processing copy before any private upload.</p></InfoPage>}
