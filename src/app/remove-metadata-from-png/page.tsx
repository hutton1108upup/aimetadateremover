import { ToolPage } from "@/components/marketing/tool-page"; import { metadataFor } from "@/lib/site";
export const metadata=metadataFor("/remove-metadata-from-png");
const faqs=[{question:"Will PNG transparency be preserved?",answer:"Yes, for files the cleaner supports. It copies the image and transparency data unchanged, then checks the new file again."},{question:"Does the tool remove every PNG text chunk?",answer:"No. It removes only the fields covered by your cleaning choice. A Software field by itself is never treated as an AI workflow."},{question:"What happens to animated PNG files?",answer:"Animation data stays in the file. If the tool cannot rewrite the PNG safely, it will not offer a download."},{question:"Is the PNG re-encoded?",answer:"No. The cleaner rewrites supported metadata chunks without decoding or re-encoding the image data."}];
export default function Page(){return <ToolPage path="/remove-metadata-from-png" label="PNG Metadata Remover" h1="Remove Metadata From PNG Images" intro="Remove supported AI prompts and workflows in your browser without re-encoding PNG image data. Transparency and color data stay in the file. Privacy Clean and Full Clean are not available yet." defaultMode="clean" acceptedFormats={["png"]} specifics={[{title:"Remove only the text you choose",body:"The tool can remove recognized prompts, parameters, and workflow chunks without wiping unrelated text by default."},{title:"Keep transparency intact",body:"Transparency data stays in place, and the tool checks the output again before download."},{title:"Rewrite PNG chunks safely",body:"Every changed chunk gets a valid checksum. If the PNG is malformed, the tool stops instead of risking a broken copy."}]} preserved={["The image data and dimensions.","Transparency, animation, and unknown safe chunks.","The ICC color profile; color-profile removal is not available in this interface."]} limits={["Compressed text stays in place when it cannot be inspected safely.","Optional removal of PNG caBX Content Credentials is off by default. C2PA is not an AI verdict.","No promise about how another service will classify the image."]} steps={[{title:"Make sure it is really a PNG",body:"The tool checks the file itself and rejects images that only have .png in the filename."},{title:"Read each chunk carefully",body:"Text, color, provenance, transparency, and animation chunks are sorted locally."},{title:"Leave unrelated chunks alone",body:"The new file leaves out only the supported fields covered by your cleaning choice."},{title:"Check the result and download",body:"The tool parses the output again before making the clean copy available."}]} faqs={faqs}/>}
