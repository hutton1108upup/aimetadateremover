import { ToolPage } from "@/components/marketing/tool-page"; import { metadataFor } from "@/lib/site";
export const metadata=metadataFor("/metadata-checker");
const faqs=[{question:"What metadata can this checker find?",answer:"It looks for supported EXIF, GPS, XMP, PNG text, generation settings, ICC profiles, and provenance data."},{question:"Does C2PA mean an image was generated by AI?",answer:"No. C2PA records where a file came from and how it was edited. Finding it does not make the image AI-generated."},{question:"Can I inspect without deleting anything?",answer:"Yes. The tool starts in inspection mode and does not change the file unless you choose to clean it."},{question:"Is the metadata uploaded?",answer:"No. The image and its metadata stay in your browser session."}];
export default function Page(){return <ToolPage path="/metadata-checker" label="Metadata Checker" h1="Image Metadata Checker for EXIF, XMP and AI Data" intro="See which prompts, workflows, GPS details, EXIF, XMP, ICC profiles, and Content Credentials are attached to your image before you remove anything." defaultMode="inspect" specifics={[{title:"A summary you can understand",body:"See why each finding matters and what you could lose by removing it."},{title:"The technical details, when you want them",body:"Open the local keys and values when you need a closer look. Prompts and locations stay hidden by default."},{title:"No made-up detector score",body:"The checker reports what is in the file. It does not turn metadata into an AI probability."}]} preserved={["Your selected file is never changed.","Full values stay hidden until you choose to reveal them.","Camera, creator, color, and provenance data are listed separately."]} limits={["It cannot see a platform's private classifier.","It cannot promise how a service will label an image.","If a file structure is unsupported or malformed, the tool says so instead of guessing."]} steps={[{title:"Check the real file type",body:"The tool reads the file itself instead of trusting the extension at the end of its name."},{title:"Scan the metadata in your browser",body:"A browser worker reads the supported parts of the file without uploading it."},{title:"Review each finding in context",body:"Labels such as Action suggested, Review first, and Informational keep the evidence separate from assumptions."},{title:"Export the report or clean a copy",body:"Download a JSON report, or make a separate clean copy and scan it again."}]} faqs={faqs}/>}
